# Morning sitrep — DISABLED.
# Rename to sitrep.yml once both TODO steps below exist. Not before: a
# scheduled workflow that does nothing still costs minutes and trust.
#
# The shape matters more than the contents:
#   1. a deterministic script decides whether anything changed;
#   2. a headless model call runs only if it did.
# The model never decides whether to wake up.

name: Morning sitrep

on:
  workflow_dispatch: {}
  # schedule:
  #   - cron: "TODO"  # 08:30 Asia/Singapore, daily. Mind which timezone cron uses.

jobs:
  sitrep:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      # TODO 1 — the deterministic check.
      # Call your change-detection script from scripts/. It must exit in a way
      # this workflow can branch on, and it must not call a model.
      # - id: check
      #   run: ...

      # TODO 2 — the report, only on change.
      # Headless Claude (`claude -p`) runs your /sitrep skill and republishes
      # dashboard.html. Guard this step on the one above.
      # - if: ...
      #   run: ...
